// G(V, E) => Graph(Vertex, Edge) => 그래프(꼭짓점, 가장자리)

// 1. 무방향 그래프
// 인접행렬 => 2차원 배열
// graph[a][b] = 1;
// graph[b][a] = 1;

// 2. 방향 그래프
// graph[a][b] = 1;

// 3. 가중치 방향그래프
// graph[a][b] = c(가중치);
